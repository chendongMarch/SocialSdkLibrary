apply plugin: 'com.android.library'


// project/build.gradle
// apply from: 'https://coding.net/u/devlab/p/Resource/git/raw/master/android/project.gradle'
def libs = rootProject.ext.libs
def version = rootProject.ext.version

android {
    compileSdkVersion version.compileSdkVersion
   
    sourceSets {
        main {
            res.srcDirs = ['src/main/res']
            jniLibs.srcDirs = ['src/main/jniLibs']
        }
    }
}

dependencies {

    // 我本地依赖是统一管理的，clone 后是无法编译的，请使用下面的依赖代替
    // compile "com.android.support:appcompat-v7:26.1.0"
    // compile 'com.parse.bolts:bolts-tasks:1.4.0'

    compile libs.appcompat_v7
    compile libs.bolts

    compile 'com.tencent.mm.opensdk:wechat-sdk-android-without-mta:5.1.4' // 微信
    compile files('libs/open_sdk_r5923_lite.jar') // QQ
    compile files('libs/weiboSDKCore_3.1.4.jar') // sina
    compile files('libs/libddshare.jar') // dingding
}